{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build --concurrency=2",
    "test": "lerna run test",
    "tsc": "lerna run tsc"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "lerna": "^8.1.8",
    "typescript": "^5.6.3"
  }
}
